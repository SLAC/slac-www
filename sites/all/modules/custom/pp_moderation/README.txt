PP Moderation module technical details.

This module adds node/%node/new-draft link that creates a draft of the node
and does not allow editing the node itself. This happens only for node types
landing page. Content types that are landing pages controlled by variable
pp_moderation_landing_page_content_type.

Also this module controls creating/moderating revisions of fieldable panel panes
added to landing page panelized node.
